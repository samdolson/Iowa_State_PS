---
title: "HW1"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r, warning = F, message = F}
# install.packages("rWishart")
library(rWishart)
library(mvtnorm)
library(Rfast)
```

$n \in \{100, 250\}$ 
$p \in \{5, 10, 50, 100, 500\}$ 
$v \in \{3, 5, 10, 30, 100\}$.

```{r}
set.seed(1e8*runif(1))

# get_samples <- function(n, df, p) {
#   
#   covmat <- rWishart(1, df = p, diag(p))
#   data - rmvt(n, sigma = diag(p), df = df)
#   data
#   
# }

nDat <- c(100, 250)
pDat <- c(5, 10, 50, 100, 500)
vDat <- c(3, 5, 10, 30, 100)

# get_samples(n = 100, df = 5, p = 3)
```